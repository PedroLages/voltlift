# VoltLift Environment Configuration
# Copy this file to .env.local and fill in your values

# =============================================================================
# Backend Configuration
# =============================================================================

# Backend type: 'firebase' or 'pocketbase'
VITE_BACKEND_TYPE=firebase

# Firebase Configuration (if using Firebase backend)
# Get these from Firebase Console: https://console.firebase.google.com/
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef

# PocketBase Configuration (if using PocketBase backend)
# VITE_POCKETBASE_URL=http://your-server:8090

# =============================================================================
# AI Features (Server-Side Only - DO NOT ADD TO CLIENT)
# =============================================================================

# ⚠️ SECURITY NOTE: GEMINI_API_KEY must be configured on the SERVER ONLY
#
# For Vercel deployment:
#   1. Go to: https://vercel.com/your-username/voltlift/settings/environment-variables
#   2. Add: GEMINI_API_KEY = your_actual_api_key_here
#   3. Select environment: Production, Preview, Development
#   4. Click "Save"
#
# For local development:
#   - NOT NEEDED - AI features will use Vercel production API endpoint
#   - If you need to test AI backend locally, add GEMINI_API_KEY to your shell:
#     export GEMINI_API_KEY=your_key_here
#
# DO NOT add VITE_GEMINI_API_KEY - this exposes the key to the client!
# The key is accessed server-side in /api/ai/suggestions.ts via process.env.GEMINI_API_KEY

# =============================================================================
# Development Settings
# =============================================================================

# Development mode (optional)
# VITE_DEV_MODE=true
